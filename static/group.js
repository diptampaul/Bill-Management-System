setTimeout(() => {
    const box = document.getElementById('ins-balance');
    // 👇️ removes element from DOM
    box.style.display = 'none';
  }, 25000); // 👈️ time in milliseconds

setTimeout(() => {
    const box = document.getElementById('money-send');
    // 👇️ removes element from DOM
    box.style.display = 'none';
  }, 20000); // 👈️ time in milliseconds

setTimeout(() => {
    const box = document.getElementById('money-send-failed');
    // 👇️ removes element from DOM
    box.style.display = 'none';
  }, 15000); // 👈️ time in milliseconds

setTimeout(() => {
    const box = document.getElementById('money-send-partially');
    // 👇️ removes element from DOM
    box.style.display = 'none';
  }, 25000); // 👈️ time in milliseconds

setTimeout(() => {
    const box = document.getElementById('payment-failed');
    // 👇️ removes element from DOM
    box.style.display = 'none';
  }, 25000); // 👈️ time in milliseconds

setTimeout(() => {
    const box = document.getElementById('payment-success');
    // 👇️ removes element from DOM
    box.style.display = 'none';
  }, 25000); // 👈️ time in milliseconds